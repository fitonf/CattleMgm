@using CattleMgm.ViewModels.Humidity;
@model HumidityCreateViewModel
@{
    ViewData["Title"] = "Regjistrimi i lageshtise";
}

<div class="card">
    <div class="card-header with-border">
        <h4>Plotesoni te dhenat me poshte</h4>
    </div>
    <form id="createForm" method="post">
        <div class="card-body">
            <div class="row">
                <div class="col-md-3">
                <label class="required" asp-for="CattleId"></label>
                @Html.DropDownListFor(m=> m.CattleId, ViewBag.Cattle as SelectList, "Zgjedh", new { @class = "form-control select2"})
                <span asp-validation-for="CattleId" class="text-danger"></span>
            </div>
            <div class="col-md-3">
                <label asp-for="Humidity"></label>
                <input asp-for="Humidity" class="form-control" />
                <span asp-validation-for="Humidity" class="text-danger"></span>  
        </div>

        </div>
        <div class="card-footer">
            <input type="submit" class="btn btn-sm btn-primary" value="Regjistro" />
        </div>
    </form>

</div>